{
"comments": [
	"an example of client config. This config is sent as response to the user login.",
	"There can be multiple configs, each config has a key.",
	"The login server selects the config by value of the field \"config\" in the user profile",
	"If the field is missing, the config with the key \"\" is used"
	],
"":{
	"client_url":"http://localhost/"	
	}
}